# Copy this to .env and fill in your values
PORT=5000
OPENAI_API_KEY=your_openai_api_key_here

# Set to "true" to use mock LLM responses (no OpenAI key needed)
USE_MOCK_LLM=true

# Set to "true" to use mock transcript (no YouTube API needed)
USE_MOCK_TRANSCRIPT=false
